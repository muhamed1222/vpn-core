# 🔑 Настройка ADMIN_API_KEY для админ-панели

## Проблема

Админ-панель требует авторизацию на бэкенде для получения данных конкурса. Нужно настроить `ADMIN_API_KEY`.

## Решение

### 1. На API сервере (Backend)

```bash
# SSH на сервер
ssh root@72.56.93.135

# Сгенерируйте случайный ключ
openssl rand -base64 32

# Или используйте простой (менее безопасно):
# echo "your-secret-admin-key-2026"

# Добавьте в .env
echo "ADMIN_API_KEY=your-generated-key-here" >> /opt/outlivion-api/.env

# Перезапустите API
sudo systemctl restart outlivion-api
```

### 2. На фронтенде (Vercel или сервер)

#### Если на Vercel:
1. Settings → Environment Variables → Add New
2. Key: `ADMIN_API_KEY`
3. Value: тот же ключ, что на бэкенде
4. Environment: Production, Preview, Development
5. Save → Redeploy

#### Если на сервере:
```bash
echo "ADMIN_API_KEY=your-generated-key-here" >> /path/to/vpnwebsite/.env
# Перезапустите Next.js
```

## 🔐 Рекомендуемый ключ

**Сгенерировать случайный:**
```bash
openssl rand -base64 32
```

**Пример:** `aB3dEfGhIjKlMnOpQrStUvWxYz1234567890AbCdEf==`

## ✅ Проверка

После настройки:
1. Админ-панель должна загружать конкурс
2. Данные участников должны отображаться
3. Экспорт должен работать

## 🆘 Если не работает

1. Проверьте, что ключ одинаковый на бэкенде и фронтенде
2. Проверьте, что API сервер перезапущен
3. Проверьте логи API: `journalctl -u outlivion-api -n 50`
